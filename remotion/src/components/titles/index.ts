import { SimpleHookTitle } from './simple-hook-title';
import { StackedHookTitle } from './stacked-hook-title';

export { SimpleHookTitle, StackedHookTitle };

// Hardcoded for now; switch this to SimpleHookTitle to swap variants.
export const ActiveHookTitle = StackedHookTitle;
